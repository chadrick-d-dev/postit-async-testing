import Posts from './Posts';

// import the rest of what you need for the tests

describe.skip('Posts', () => {
  it('Should render a container', () => {
    // psedueocode the steps to this test 
    
    // then write the test out
  });

  it('Should render a list of posts', () => {
    // psedueocode the steps to this test 
    
    // then write the test out
  });
});
