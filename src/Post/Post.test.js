import React from 'react';
import Post from './Post';

describe.skip('Post', () => {
  it('Should render a post correctly', () => {
  
  });

  it('Should increment its score', () => {
  
  });

  it('Should decrement its scrore', () => {
  
  });
});
